{
  "name": "hectos-website-root",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "next": "^14.0.4"
  },
  "scripts": {
    "dev": "cd front && npm run dev",
    "build": "cd front && npm install && npm run build",
    "start": "cd front && npm run start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
