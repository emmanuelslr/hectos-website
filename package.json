{
  "name": "hectos-website-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd front && npm run dev",
    "build": "cd front && npm run build",
    "start": "cd front && npm run start",
    "install:all": "cd front && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
