{
  "buildCommand": "cd front && npm install && npm run build",
  "installCommand": "cd front && npm install",
  "framework": "nextjs",
  "monorepo": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/front/$1"
    }
  ]
}
